This directory is reserved for generated API clients/types (for example via `oapi-codegen` from `../public/openapi.json`).

The initial CLI implementation uses hand-written API envelopes and domain types to keep the first release focused and easy to review.
